const facePoints = {
    // lipsUpper: scaleCoord(this.face.annotations.lipsUpperOuter[5]),
    // lipsLower: scaleCoord(this.face.annotations.lipsLowerOuter[4]),
    
    // rightEyeU: scaleCoord(this.face.annotations.rightEyeUpper0[3]),
    // rightEyeL: scaleCoord(this.face.annotations.rightEyeLower0[4]),

    // leftEyeU: scaleCoord(this.face.annotations.leftEyeUpper0[3]),
    // leftEyeL: scaleCoord(this.face.annotations.leftEyeLower0[4]),

    // betweenEyes: scaleCoord(this.face.annotations.midwayBetweenEyes[0]),

    // leftCheek: scaleCoord(this.face.annotations.leftCheek[0]),
    // rightCheek: scaleCoord(this.face.annotations.rightCheek[0]),

    // nose: scaleCoord(this.face.scaledMesh[5]),

    // topLeft: scaleCoord(this.face.boundingBox.topLeft),
    // bottomRight: scaleCoord(this.face.boundingBox.bottomRight),
    // w: bottomRight.x - topLeft.x,
    // faceDia: w,
    // topFaceDia: w / 2,
    // dia: w / 8
}

export default facePoints;